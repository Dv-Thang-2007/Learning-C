#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

struct hello
{
    /* data */
};
typedef struct hello hl;

void input(){
    printf("nhap : ");
    fgets(0, sizeof(0), stdin);
    0[strcspn(0, "\n")] = '\0';

}
void output(){
    printf("nhap : ");
    fgets(0, sizeof(0), stdin);
    0[strcspn(0, "\n")] = '\0';

}
int main(){

    return 0;
}